fzf is a general-purpose command-line fuzzy finder. The details can be found [there](https://github.com/junegunn/fzf)

# installation

```shell
brew install fzf
```

Then, add the following to `.zshrc`

```shell 
source <(fzf --zsh)
```